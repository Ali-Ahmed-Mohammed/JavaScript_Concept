
// Array is an object that can store multiple values at once.

let words = ['hello', 'world', 'welcome']

// instead of writing our code like this

let word = 'helo';
let word2 = 'world';
let world3 = 'welcome';

// in the previous example the "word" is an array. the array is storing 3 values.


// =============== Create an array

// you can create an array in two ways

// 1: using array literal

const array1 = ['food', 'cloth'];

// 2: using the new